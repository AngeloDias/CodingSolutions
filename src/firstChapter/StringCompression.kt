package firstChapter

class StringCompression {

    fun compressString(original: String): String {
        var compressed = ""
        var countChars = 0
        var tempChar = original[0]

        return compressed
    }

}
